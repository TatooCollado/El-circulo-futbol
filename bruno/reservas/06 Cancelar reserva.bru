meta {
  name: 06 Cancelar reserva
  type: http
  seq: 6
}

put {
  url: {{baseUrl}}/reservas/{{reservaId}}/cancelar
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.reserva.estado: eq cancelada
}

