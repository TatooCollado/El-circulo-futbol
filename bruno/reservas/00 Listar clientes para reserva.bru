meta {
  name: 00 Listar clientes para reserva
  type: http
  seq: 0
}
get {
  url: {{baseUrl}}/reservas/clientes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.clientes: isArray
}
