meta {
  name: 03 Listar reservas admin
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/reservas
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.reservas: isArray
}

