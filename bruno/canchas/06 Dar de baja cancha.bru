meta {
  name: 06 Dar de baja cancha
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/canchas/{{canchaId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.cancha.disponible: eq false
}

