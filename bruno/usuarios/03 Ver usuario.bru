meta {
  name: 03 Ver usuario
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/users/{{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.user.id: isDefined
}

