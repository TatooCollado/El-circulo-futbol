meta {
  name: 06 Listar clientes admin
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/users/clientes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.clientes: isArray
}
