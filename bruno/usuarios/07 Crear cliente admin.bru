meta {
  name: 07 Crear cliente admin
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/users/clientes
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "nombre": "Cliente",
    "apellido": "Admin",
    "email": "cliente.admin{{$timestamp}}@demo.com",
    "password": "123456"
  }
}

vars:post-response {
  usuarioIdCliente: res.body.cliente.id
}

assert {
  res.status: eq 201
  res.body.cliente.email: isString
}
