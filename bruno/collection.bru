docs {
  Coleccion Bruno para probar la API REST de El Circulo Futbol.

  Usar el environment "Local".
  Primero ejecutar un Login para guardar la variable runtime "token".
  Las requests protegidas usan ese token como Bearer Token.
}

